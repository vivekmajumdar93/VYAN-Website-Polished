'use client'
// Intentionally a no-op now. The bright bloomed core plane was creating an intense halo
// that washed the void. Galaxy luminosity is now driven entirely by the dense particle
// distribution + the small in-shader uCoreGlow sheen.
export default function Core() {
  return null
}
