'use client'
// Orbit trail rings are now intentionally omitted to match the clean disk reference.
// Kept as an exported no-op so existing imports continue to work.
export default function OrbitTrails() {
  return null
}
