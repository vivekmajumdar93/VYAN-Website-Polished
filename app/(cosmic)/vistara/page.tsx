// /vistara — the Product sub-void (Vistāra). Canvas lives in shared (cosmic) layout.
export const metadata = { title: 'Vistāra — The Unfurling of VYAN' };

export default function VistaraPage() {
  return null;
}
