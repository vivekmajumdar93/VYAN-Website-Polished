// /vyoma — the cosmic gateway. The canvas is in the shared (cosmic) layout;
// this page exists only so Next.js renders the route and the layout can
// observe the pathname change to set the realm to 'gateway'.
export const metadata = { title: 'Vyōma — VYAN Gateway' };

export default function VyomaPage() {
  return null;
}
