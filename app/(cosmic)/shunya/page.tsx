// /shunya — the parent void with 5 orbs. Canvas lives in shared layout.
export const metadata = { title: 'Shunya Mandala — The Void Between' };

export default function ShunyaPage() {
  return null;
}
